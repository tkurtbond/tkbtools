/* tkbmswoe.h - header for tkb's useful mswindows functions. */

/* Print the last MS windows error. */
void print_error (char *label);

/* Print the last MS windows error and die. */
void fatal_error (char *label);
